package main

import "fmt"

func main() {
	var limit int
	fmt.Println("Enter the limit")
	fmt.Scan(&limit)
	fmt.Println("")
	for i := 1; i <= limit; i++ {
		for j := 1; j <= i; j++ {
			fmt.Print(j, "\t")
		}
		fmt.Print("\n")
	}
}
